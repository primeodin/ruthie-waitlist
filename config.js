/* Public client config for the waitlist form.
 *
 * Do not put GitHub tokens, PATs, or server secrets here.
 * A Web3Forms access key is designed to be public in HTML — it is an
 * alias to an inbox, not a credential. Get one (free, no card) at
 * https://web3forms.com and paste it below, then push to main.
 */
window.RUTHIE_WAITLIST = {
  web3formsKey: ""
};
